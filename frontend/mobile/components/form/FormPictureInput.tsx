import { UseFormReturn } from "react-hook-form";
import { Text } from "react-native";

export default function FormPictueInput({ name, placeholder, form }: { name: string, placeholder: string, form: UseFormReturn<any> }) {
  return <Text>WIP</Text>
}
