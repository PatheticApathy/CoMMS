version: "2"
sql:
  - engine: sqlite
    queries: ./queries/
    schema: ./migrations/
    gen:
      go:
        emit_json_tags: true
        package: materialdb
        out: ../../pkg/databases/materialdb
